#!/usr/bin/env bash
set -euo pipefail

# Project-local startup hooks for this container session.
# Keep this file committed as a template, and customize
# .devcontainer/session-start.sh (gitignored) per project.

if [ -n "${AGENT_ALLOWED_ROOT:-}" ]; then
  cd "$AGENT_ALLOWED_ROOT"
fi

# Examples:
# npm ci
# pip install -r requirements.txt
# make dev-setup
